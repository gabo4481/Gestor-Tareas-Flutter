import 'dart:ui';

class AppColor {
  // Paleta principal azul
  static const Color primary = Color(0xFF2196F3);  // Azul vibrante
  static const Color primaryDark = Color(0xFF0D47A1);  // Azul oscuro profundo
  static const Color primaryLight = Color(0xFF90CAF9);  // Azul claro suave

  // Colores de acento y soporte
  static const Color accent = Color(0xFFFF9800);  // Naranja energético
  static const Color secondary = Color(0xFF4CAF50);  // Verde fresco
  static const Color error = Color(0xFFF44336);  // Rojo para acciones críticas

  // Fondos y superficies
  static const Color background = Color(0xFFF5F9FF);  // Azul blanquecino muy claro
  static const Color surface = Color(0xFFFFFFFF);  // Blanco puro
  static const Color card = Color(0xFFE3F2FD);  // Azul glaciar claro

  // Textos y elementos UI
  static const Color textPrimary = Color(0xFF212121);  // Negro suave
  static const Color textSecondary = Color(0xFF757575);  // Gris medio
  static const Color textDisabled = Color(0xFFBDBDBD);  // Gris claro
  static const Color divider = Color(0xFFBDBDBD);  // Líneas divisorias
  
  // Estados y feedback
  static const Color selected = Color(0xFFE3F2FD);  // Azul muy claro
  static const Color hover = Color(0xFFBBDEFB);  // Azul pastel
  static const Color focused = Color(0xFF64B5F6);  // Azul medio
}